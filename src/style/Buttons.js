import styled from "styled-components";

const Buttons = styled.button`
  color: ${({ color }) => color};
  width: ${({ width }) => width};
  height: ${({ width }) => width};
`;
export default Buttons;
